version: "3.8"

services:
  app:
    # Asegúrate de que la imagen coincida con la que construyes en el Action
    image: ghcr.io/leonardo22f/tarea8edisonflores:latest 
    deploy:
      replicas: 2
      restart_policy:
        condition: on-failure
    ports:
      - "5000:5000"
    networks:
      - webnet

networks:
  webnet: