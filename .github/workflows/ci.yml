name: Deploy Pro

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: secret

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Copiar stack.yml al servidor
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          port: ${{ secrets.VPS_SSH_PORT }}
          source: "stack.yml"
          target: "/home/${{ secrets.VPS_USER }}/deploy/"

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}
          port: ${{ secrets.VPS_SSH_PORT }}
          script: |
            echo "--- 🚀 DEPLOY LIMPIO PARA ${{ secrets.STACK_NAME }} ---"

            mkdir -p /home/${{ secrets.VPS_USER }}/deploy

            echo "🔑 Logueando GHCR..."
            echo "${{ secrets.GHCR_PAT }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            echo "📥 Pull de la última imagen: ${{ secrets.IMAGE_NAME }}:latest"
            docker pull ${{ secrets.IMAGE_NAME }}:latest

            echo "🔥 Eliminando stack anterior..."
            docker stack rm ${{ secrets.STACK_NAME }} || true

            echo "⏳ Esperando 10s..."
            sleep 10

            echo "✨ Desplegando stack..."
            docker stack deploy -c /home/${{ secrets.VPS_USER }}/deploy/stack.yml ${{ secrets.STACK_NAME }}

            echo "📌 Listando servicios:"
            docker stack services ${{ secrets.STACK_NAME }}
